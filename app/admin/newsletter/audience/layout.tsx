import type { ReactNode } from "react";

export const metadata = {
  title: "Newsletter audience · Admin",
};

export default function AdminNewsletterAudienceLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
